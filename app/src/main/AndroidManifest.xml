<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.cz2006_mappy">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InsertToken"
            android:label="@string/title_activity_insert_token"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".MyPurchases"
            android:label="@string/title_activity_my_purchases"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ConvertToCashNew"
            android:label="@string/title_activity_convert_to_cash_new"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SeeMoreCashIncentives" />
        <activity android:name=".FeedbackForm" />
        <activity android:name=".createItemActivity" />
        <activity android:name=".ItemDetailActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>